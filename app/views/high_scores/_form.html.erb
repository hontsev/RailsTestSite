<%= form_for(@high_score,:html=>{:class=>"form-horizontal"}) do |f| %>
  <% if @high_score.errors.any? %>
    <div id="error_explanation" >
      <h2><%= pluralize(@high_score.errors.count, "error") %> prohibited this high_score from being saved:</h2>

      <ul>
      <% @high_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  

    <div class="form-group field">
    <%= f.label :game ,:class=>"control-label col-sm-2"%>
    <div class="col-sm-10">
      <%= f.text_field :game ,:class=>"form-control",:style=>"max-width:40%"%>
    </div>
  </div>
  <div class="form-group field">
    <%= f.label :score ,:class=>"control-label col-sm-2"%>
     <div class="col-sm-10">
      <%= f.number_field :score ,:class=>"form-control",:style=>"max-width:40%" %>
    </div>
  </div>
  <div class="form-group actions">
    
    <%= f.submit :class=>"btn btn-success",:style=>"margin-left:20%" %>
  </div>


<% end %>
